import java.io.*;

/*public class LeitorSerializados {
  public <T extends IDefault> T ler(String nomeArquivo) {
    try {
      var arquivo = new FileInputStream(nomeArquivo);
      var input   = new ObjectInputStream(arquivo);
      return (T)input.readObject();
    } catch (Exception e) {
      return new T();
    }
  }
}
*/
